pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "dev.flutter.flutter-plugin-loader" version "1.0.0"
    }
}

plugins {
    id "dev.flutter.flutter-plugin-loader"
}

include ":app"
